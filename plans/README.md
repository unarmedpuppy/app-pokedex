# Implementation Plans — pokedex

Generated by the improve skill (advisor audit, `quick` effort) on 2026-07-07,
against commit `1a43c12`. Non-interactive run: plans were written for the top
findings by leverage (default per the skill when no user is available to
choose). Execute in the order below. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Scope note: this audit covered the custom Pokemon HOME collection app
(`collection/`, `Dockerfile`, `docker-compose.yml`, CI) plus deploy config in
`home-server/apps/pokedex/`. The upstream veekun fork (`pokedex/`, `scripts/`,
`bin/`) and the host-side capture tooling (`collection/capture.py`,
`parse.py`, `batch_*.py`) were NOT audited.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Reconcile deploy config before first `v*` tag | P1 | S | — | TODO |
| 002 | Contain `/image/{path}` path traversal | P1 | S | — | TODO |
| 003 | Conventions alignment (CORS, /health, Dockerfile, AGENTS.md) | P2 | M | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003 depends on 001 because shipping the hardened Dockerfile requires a `v*`
  tag, and the tag must not fire until the home-server compose expects port
  8420 (otherwise the live site goes down — see plan 001).
- 002 is independent and can run in parallel with either.

## Findings considered and rejected / deferred

- **No verification baseline for the collection app** (zero tests; the veekun
  `pokedex/tests/` suite needs a built sqlite DB; `.gitea/workflows/test.yml`
  is a hello-world stub): real, but at ~150 LOC of API code the seed tests in
  plan 002 are the proportionate response. Revisit if the app grows.
- **SQLite connections not closed on exception paths** (`collection/api.py:45-80`
  builds/closes conns without try/finally): cosmetic at this traffic level;
  fold into any future refactor, not worth a standalone plan.
- **`.gitignore` secrets coverage**: already done on `security/gitignore-secrets`,
  merged at `1a43c12` — known, tracked in the 2026-07-02 remediation tracker.
- **CI `secrets: inherit` + shared workflow pinned `@main`**: ecosystem-wide
  finding S2/S3 in SECURITY-AUDIT-2026-07-02 — known, tracked centrally, not
  re-reported per-repo.
- **Data sync for `pokemon_home.db` to the server**: needed for the deployed
  app to show anything (capture runs on a workstation), but it is a design
  decision (rsync cron? upload endpoint?) for the owner — deferred, noted in
  plans 001/003 maintenance sections.
